# Go backend API base URL. Include https:// in production.
NEXT_PUBLIC_API_BASE_URL=https://parkintel-production.up.railway.app

# Optional Mapbox GL JS public token. Leave blank to use the built-in fallback map.
NEXT_PUBLIC_MAPBOX_TOKEN=
